apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: congen

resources:
- ../../base

patches:
- path: patches/keycloak-deployment-patch.yaml
  target:
    kind: Deployment
    name: keycloak 