apiVersion: v1
kind: Service
metadata:
  name: memcached
  namespace: congen
  labels:
    app: memcached
spec:
  type: ClusterIP
  ports:
  - port: 11211
    targetPort: 11211
    protocol: TCP
    name: memcached
  selector:
    app: memcached
