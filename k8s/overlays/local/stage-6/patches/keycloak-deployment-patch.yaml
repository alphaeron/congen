apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: keycloak
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: keycloak
        envFrom:
        - secretRef:
            name: keycloak-secret
        args: ["start-dev"] 