build:
  maxIssues: 0
  weights:
    style: 1

processors:
  active: false

console-reports:
  active: false

output-reports:
  active: true

style:
  active: true
  UnusedImports:
    active: true
    severity: error
  WildcardImport:
    active: true
    severity: error

comments:
  active: false

naming:
  active: false

performance:
  active: false

potential-bugs:
  active: false

complexity:
  active: false 