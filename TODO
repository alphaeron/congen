linting

pass integration tests

pass unit tests

need to unify migration documentation, and put in resources/migrations/README.md





fix weight rounding issue to nearest plate

exercise movement type enum?

use snake_case for api values

band weight for de exercises.




Secondary movement selection should pick an exercise simlar to the primary movement.




put generateWorkoutsForWeek in the template itself.

2 or 3 week conjugate generation

Add the ability for a user to enter their weak muscles, and have the accessories focus on those.

Ensure accommodated resistance taken into account.  Check wenning videos for other laws to include too

If muscle imbalance, prefer unilateral accessories




don't hardcode default weight of 50 lbs?

function to estimate default weight if missing based on other user 1rms?




then move hardcoded program templates into database models to add flexability.

then support adding custom movementType s so there is complete flexability with what type of workout you want to generate

then support exerciseAttributes instead of isUnilateral, ... (like tags) for even more flexability.

remove unneeded endpoints

pull values from application.properties from k8s manifest.  This way, we only need 1 application.properties, and the k8s manifests will specify differences.